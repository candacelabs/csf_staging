load("@rules_cc//cc:cc_library.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

exports_files(["lib/tree_sitter.ml", "lib/tree_sitter.mli"])

cc_library(
    name = "raw_runtime",
    srcs = ["lib/tree_sitter_runtime.c"],
    hdrs = glob(["include/**/*.h", "vendor/tree-sitter/**/*.h"]),
    textual_hdrs = glob(["vendor/tree-sitter/**/*.c"]),
    includes = ["include", "vendor/tree-sitter"],
    copts = ["-std=c11", "-D_DEFAULT_SOURCE"],
    linkopts = ["-ldl", "-lpthread"],
    linkstatic = True,
)

cc_library(
    name = "stubs",
    srcs = ["lib/tree_sitter_stubs.c"],
    linkstatic = True,
    deps = [
        ":raw_runtime",
        "@opam.ocamlsdk//ffi/lib",
    ],
)
