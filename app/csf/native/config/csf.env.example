# Leave both values empty for the minimal dashboard-only CSF service.
# Set both to enable PostgreSQL-backed knowledge and OpenSearch indexing.
CSF_DATABASE_CONFIG=
CSF_SEARCH_URL=
CSF_SEARCH_INDEX=brain-knowledge
