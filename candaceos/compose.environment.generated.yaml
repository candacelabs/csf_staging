# Code generated from Candacefile by tools/candace_environment.py; DO NOT EDIT.
# Regenerate with: python3 tools/candace_environment.py write
services:
  postgres:
    environment:
      POSTGRES_DB: "${POSTGRES_DB:-candaceos}"
      POSTGRES_USER: "${POSTGRES_USER:-candaceos}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-}"
  warden:
    environment:
      WARDEN_CONFIG: "${WARDEN_CONFIG:-/etc/warden/warden.yaml}"
      WARDEN_LOG_FORMAT: "${WARDEN_LOG_FORMAT:-console}"
  agent-dry-run:
    environment:
      CANDACEOS_AGENT_BIND: "${CANDACEOS_AGENT_BIND:-0.0.0.0:8094}"
      CANDACEOS_AGENT_TOKEN: "${CANDACEOS_AGENT_TOKEN:-}"
      CANDACEOS_AGENT_NODE_ID: "${CANDACEOS_AGENT_NODE_ID:-candaceos-demo}"
      CANDACEOS_AGENT_STATE_FILE: "${CANDACEOS_AGENT_STATE_FILE:-/var/lib/candaceos-agent/state.json}"
      CANDACEOS_AGENT_REVISION_ROOT: "${CANDACEOS_AGENT_REVISION_ROOT:-/var/lib/candaceos-agent/revisions}"
      CANDACEOS_AGENT_REVISION_MAX_ENTRIES: "${CANDACEOS_AGENT_REVISION_MAX_ENTRIES:-}"
      CANDACEOS_AGENT_REVISION_MAX_BYTES: "${CANDACEOS_AGENT_REVISION_MAX_BYTES:-}"
      DOCKER_CONFIG: "${DOCKER_CONFIG:-/tmp/docker-config}"
      CANDACEOS_AGENT_WORKSPACE: "${CANDACEOS_AGENT_DRY_WORKSPACE:-/workspace}"
      CANDACEOS_AGENT_DRY_RUN: "${CANDACEOS_AGENT_DRY_RUN_ENABLED:-true}"
  agent-live:
    environment:
      CANDACEOS_AGENT_BIND: "${CANDACEOS_AGENT_BIND:-0.0.0.0:8094}"
      CANDACEOS_AGENT_TOKEN: "${CANDACEOS_AGENT_TOKEN:-}"
      CANDACEOS_AGENT_NODE_ID: "${CANDACEOS_AGENT_NODE_ID:-candaceos-demo}"
      CANDACEOS_AGENT_STATE_FILE: "${CANDACEOS_AGENT_STATE_FILE:-/var/lib/candaceos-agent/state.json}"
      CANDACEOS_AGENT_REVISION_ROOT: "${CANDACEOS_AGENT_REVISION_ROOT:-/var/lib/candaceos-agent/revisions}"
      CANDACEOS_AGENT_REVISION_MAX_ENTRIES: "${CANDACEOS_AGENT_REVISION_MAX_ENTRIES:-}"
      CANDACEOS_AGENT_REVISION_MAX_BYTES: "${CANDACEOS_AGENT_REVISION_MAX_BYTES:-}"
      DOCKER_CONFIG: "${DOCKER_CONFIG:-/tmp/docker-config}"
      CANDACEOS_AGENT_WORKSPACE: "${CANDACEOS_AGENT_LIVE_WORKSPACE:-/workspace}"
      CANDACEOS_AGENT_DRY_RUN: "${CANDACEOS_AGENT_DRY_RUN_DISABLED:-false}"
  copilot:
    environment:
      COPILOT_GITHUB_TOKEN: "${COPILOT_GITHUB_TOKEN:-}"
      GH_TOKEN: "${COPILOT_GITHUB_TOKEN:-}"
      GITHUB_TOKEN: "${COPILOT_GITHUB_TOKEN:-}"
      COPILOT_CONNECTION_TOKEN: "${CANDACEOS_COPILOT_CONNECTION_TOKEN:-}"
      COPILOT_HOME: "${CANDACEOS_COPILOT_HOME:-/var/lib/copilot}"
      HOME: "${CANDACEOS_COPILOT_HOME:-/var/lib/copilot}"
  opencode:
    environment:
      OPENCODE_SERVER_USERNAME: "${CANDACEOS_OPENCODE_USERNAME:-opencode}"
      OPENCODE_SERVER_PASSWORD: "${CANDACEOS_OPENCODE_PASSWORD:-}"
      OPENAI_API_KEY: "${OPENAI_API_KEY:-}"
      ANTHROPIC_API_KEY: "${ANTHROPIC_API_KEY:-}"
      OPENROUTER_API_KEY: "${OPENROUTER_API_KEY:-}"
  core:
    environment:
      HOME: "${CANDACEOS_DATA_DIR:-/var/lib/candaceos}"
      CANDACEOS_DATA_DIR: "${CANDACEOS_DATA_DIR:-/var/lib/candaceos}"
      CANDACEOS_DATABASE_URL: "${CANDACEOS_DATABASE_URL:-}"
      CANDACEOS_HARNESS_BACKEND: "${CANDACEOS_HARNESS_BACKEND:-copilot-cli}"
      CANDACEOS_BIND: "${CANDACEOS_BIND:-0.0.0.0:7780}"
      CANDACEOS_WORKSPACE: "${CANDACEOS_WORKSPACE:-/workspace}"
      CANDACEOS_WARDEN_URL: "${CANDACEOS_WARDEN_URL:-http://127.0.0.1:7717}"
      CANDACEOS_AGENT_URL: "${CANDACEOS_AGENT_URL:-}"
      CANDACEOS_AGENT_PORT: "${CANDACEOS_AGENT_PORT:-8094}"
      CANDACEOS_AGENT_TOKEN: "${CANDACEOS_AGENT_TOKEN:-}"
      CANDACEOS_NODE_LABELS: "${CANDACEOS_NODE_LABELS:-{}}"
      CANDACEOS_APPROVAL_TIMEOUT: "${CANDACEOS_APPROVAL_TIMEOUT:-15m}"
      CANDACEOS_FLEET_POLL_INTERVAL: "${CANDACEOS_FLEET_POLL_INTERVAL:-2s}"
      CANDACEOS_COPILOT_CLI: "${CANDACEOS_COPILOT_CLI:-/usr/local/bin/copilot}"
      CANDACEOS_COPILOT_URL: "${CANDACEOS_COPILOT_URL:-}"
      CANDACEOS_COPILOT_CONNECTION_TOKEN: "${CANDACEOS_COPILOT_CONNECTION_TOKEN:-}"
      CANDACEOS_COPILOT_MODEL: "${CANDACEOS_COPILOT_MODEL:-gpt-5.4}"
      CANDACEOS_OLLAMA_URL: "${CANDACEOS_OLLAMA_URL:-}"
      CANDACEOS_OLLAMA_MODEL: "${CANDACEOS_OLLAMA_MODEL:-}"
      CANDACEOS_OLLAMA_MODEL_DIGEST: "${CANDACEOS_OLLAMA_MODEL_DIGEST:-}"
      CANDACEOS_OLLAMA_CONTEXT_TOKENS: "${CANDACEOS_OLLAMA_CONTEXT_TOKENS:-16384}"
      CANDACEOS_OLLAMA_MAX_TOOL_CALLS: "${CANDACEOS_OLLAMA_MAX_TOOL_CALLS:-16}"
      CANDACEOS_OLLAMA_TURN_TIMEOUT: "${CANDACEOS_OLLAMA_TURN_TIMEOUT:-10m}"
      CANDACEOS_OPENCODE_URL: "${CANDACEOS_OPENCODE_URL:-http://127.0.0.1:4096}"
      CANDACEOS_OPENCODE_USERNAME: "${CANDACEOS_OPENCODE_USERNAME:-opencode}"
      CANDACEOS_OPENCODE_PASSWORD: "${CANDACEOS_OPENCODE_PASSWORD:-}"
      CANDACEOS_OPENCODE_SESSION_ID: "${CANDACEOS_OPENCODE_SESSION_ID:-}"
      CANDACEOS_OPENCODE_REQUEST_TIMEOUT: "${CANDACEOS_OPENCODE_REQUEST_TIMEOUT:-10s}"
      CANDACEOS_OPENCODE_POLL_INTERVAL: "${CANDACEOS_OPENCODE_POLL_INTERVAL:-1s}"
      CANDACEOS_OPENCODE_QUEUE_CAPACITY: "${CANDACEOS_OPENCODE_QUEUE_CAPACITY:-32}"
      CANDACEOS_OPENCODE_MODEL: "${CANDACEOS_OPENCODE_MODEL:-}"
