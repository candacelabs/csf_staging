// Code generated by pkg/widget/internal/uigen from the RelayPipeline widget document. DO NOT EDIT.

package relaypipeline

import "github.com/candacelabs/csf/pkg/gotth/live"

// RelayPipelineView draws the RelayPipeline widget's default live region.
templ RelayPipelineView(state RelayPipelineState) {
	@RelayPipelineViewAt(state, RelayPipelineRegion)
}

// RelayPipelineViewAt draws one instance in its host-assigned live region.
//
// Every value in it is a pure read of state and region: equal inputs render
// byte-identical markup: that comparison is what suppresses a patch nobody needs.
templ RelayPipelineViewAt(state RelayPipelineState, region string) {
	{{ titleID := region + "-title" }}
	<aside { live.Region(region)... } class="widget" aria-labelledby={ titleID } data-widget="RelayPipeline" data-palette="fieldStation" data-motion={ state.MotionActiveText() }>
		<header class="widget-chrome">
			<p class="widget-source widget-token-muted">{ state.SourceLabel() }</p>
			<h2 class="widget-title widget-token-ink" id={ titleID }>{ state.TitleLabel() }</h2>
		</header>
		<svg class="widget-scene" id={ state.MotionTickIDAt(region) } viewBox="0 0 100 100" preserveAspectRatio="xMidYMid meet" role="img" aria-label={ state.SceneDescriptionLabel() }>
			<g class="widget-edge" transform="translate(12,50) rotate(0)" style="--widget-edge-length: 38px">
				<line class="widget-edge-line widget-token-rule" x1="0" y1="0" x2="38" y2="0"></line>
				<circle class="widget-pulse widget-pulse-forward widget-token-accent" r="2" style="--widget-pulse-duration: 640ms; --widget-pulse-delay: 0ms"></circle>
			</g>
			<g class="widget-edge" transform="translate(50,50) rotate(0)" style="--widget-edge-length: 38px">
				<line class="widget-edge-line widget-token-rule" x1="0" y1="0" x2="38" y2="0"></line>
				<circle class="widget-pulse widget-pulse-forward widget-token-accent" r="2" style="--widget-pulse-duration: 640ms; --widget-pulse-delay: 200ms"></circle>
			</g>
			<g class="widget-node widget-role-stage" transform="translate(12,50)">
				<circle class="widget-marker widget-marker-small widget-token-accent" r="5"></circle>
				<text class="widget-node-title widget-token-ink" y="-10" text-anchor="middle">{ state.IngressLabel() }</text>
				<text class="widget-node-caption widget-token-muted" y="15" text-anchor="middle">{ state.IngressCaptionLabel() }</text>
			</g>
			<g class="widget-node widget-role-stage" transform="translate(50,50)">
				<circle class="widget-emphasis widget-token-accent" r="7" style="--widget-emphasis-duration: 560ms; --widget-emphasis-delay: 120ms"></circle>
				<circle class="widget-marker widget-marker-small widget-token-accent" r="5"></circle>
				<text class="widget-node-title widget-token-ink" y="-10" text-anchor="middle">{ state.RelayLabel() }</text>
				<text class="widget-node-caption widget-token-muted" y="15" text-anchor="middle">{ state.RelayCaptionLabel() }</text>
			</g>
			<g class="widget-node widget-role-stage" transform="translate(88,50)">
				<circle class="widget-marker widget-marker-small widget-token-accent" r="5"></circle>
				<text class="widget-node-title widget-token-ink" y="-10" text-anchor="middle">{ state.SinkLabel() }</text>
				<text class="widget-node-caption widget-token-muted" y="15" text-anchor="middle">{ state.SinkCaptionLabel() }</text>
			</g>
		</svg>
		<ul class="widget-legend">
			<li class="widget-legend-entry widget-token-muted"><span class="widget-swatch widget-token-accent" aria-hidden="true"></span>{ state.RecordLegendLabel() }</li>
		</ul>
		<ul class="widget-stats">
			<li class="widget-stat widget-token-ink">{ state.CursorStatLabel() }</li>
			<li class="widget-stat widget-token-ink">{ state.BacklogStatLabel() }</li>
		</ul>
	</aside>
}
