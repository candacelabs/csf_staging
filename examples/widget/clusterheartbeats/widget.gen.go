// Code generated by pkg/widget/internal/uigen from the ClusterHeartbeats widget document. DO NOT EDIT.

// Package clusterheartbeats is the generated ClusterHeartbeats widget.
//
// Edit the widget document and regenerate; edits here are overwritten.
package clusterheartbeats

import (
	"context"
	"strconv"

	"github.com/a-h/templ"

	"github.com/candacelabs/csf/pkg/gotth/live"
	"github.com/candacelabs/csf/pkg/widget"
)

// This widget's identity. The region is the only one that crosses the wire: a
// patch names it, so changing it is a client-visible change. The palette is a
// name a host resolves to values; this widget carries no colour of its own.
// The title id is what the widget's landmark is labelled by.
const (
	ClusterHeartbeatsName    = "ClusterHeartbeats"
	ClusterHeartbeatsRegion  = "widget.cluster-heartbeats"
	ClusterHeartbeatsPalette = "fieldStation"
	ClusterHeartbeatsTitleID = "widget.cluster-heartbeats-title"
)

// The wire names this widget accepts. The set is exhaustive and default-deny:
// a name absent from the registration is refused before any reducer runs.
const (
	ClusterHeartbeatsEventToggleMotion = "widget.cluster.motion-toggle"
	ClusterHeartbeatsEventSnapshot     = "widget.cluster.snapshot"
)

// The wire field names each event carries. They are the other half of an
// event's contract: the widget reads these out of a delivery, and whatever
// produced the delivery wrote them in. Registration.Payloads carries the same
// names as data, for a host that fills an event it cannot name at compile time.
const (
	ClusterHeartbeatsEventSnapshotFieldSequence      = "sequence"
	ClusterHeartbeatsEventSnapshotFieldConnected     = "connected"
	ClusterHeartbeatsEventSnapshotFieldAuthoritative = "authoritative"
	ClusterHeartbeatsEventSnapshotFieldLeaderKnown   = "leader_known"
	ClusterHeartbeatsEventSnapshotFieldHasQuorum     = "has_quorum"
	ClusterHeartbeatsEventSnapshotFieldTerm          = "term"
	ClusterHeartbeatsEventSnapshotFieldVoters        = "voters"
	ClusterHeartbeatsEventSnapshotFieldAliveVoters   = "alive_voters"
)

// ClusterHeartbeatsState is this widget's own state, and the type argument every
// method below is written in. The host holds it without knowing it; nothing
// in this file asserts it back, because nothing in this file ever loses it.
type ClusterHeartbeatsState struct {
	// Sequence is the "sequence" state field, declared type counter.
	Sequence uint64

	// Connected is the "connected" state field, declared type flag.
	Connected bool

	// Authoritative is the "authoritative" state field, declared type flag.
	Authoritative bool

	// LeaderKnown is the "leaderKnown" state field, declared type flag.
	LeaderKnown bool

	// HasQuorum is the "hasQuorum" state field, declared type flag.
	HasQuorum bool

	// Paused is the "paused" state field, declared type flag.
	Paused bool

	// Term is the "term" state field, declared type count.
	Term int64

	// Voters is the "voters" state field, declared type count.
	Voters int64

	// AliveVoters is the "aliveVoters" state field, declared type count.
	AliveVoters int64

	// Degraded is the "degraded" state field, declared type flag.
	Degraded bool
}

// ClusterHeartbeats is the generated ClusterHeartbeats widget.
//
// I is the HOST's identity type, threaded through and never read: a widget
// document names no host, no address and no credential, so nothing here can
// look at an identity. It is a type parameter because the SDK's contract
// carries one — live.Session stopped erasing the application's identity type
// on 2026-09-03 — and a generated widget must fit whatever host registers it.
type ClusterHeartbeats[I live.IIdentity] struct {
	region string
}

// NewClusterHeartbeats returns the widget a host registers, instantiated on that
// host's own identity type.
func NewClusterHeartbeats[I live.IIdentity]() *ClusterHeartbeats[I] {
	return NewClusterHeartbeatsAt[I](ClusterHeartbeatsRegion)
}

// NewClusterHeartbeatsAt gives one instance its host-assigned region. The host validates
// region syntax and uniqueness when assembling its live configuration.
func NewClusterHeartbeatsAt[I live.IIdentity](region string) *ClusterHeartbeats[I] {
	return &ClusterHeartbeats[I]{region: region}
}

// The contract, asserted at one instantiation. Anonymous is the identity a
// host with no accounts uses, and any other I satisfies the same interfaces:
// nothing below branches on it.
var (
	_ widget.IWidget[ClusterHeartbeatsState, live.AnonymousIdentity] = (*ClusterHeartbeats[live.AnonymousIdentity])(nil)
	_ widget.IDirtyDeclarer[ClusterHeartbeatsState]                  = (*ClusterHeartbeats[live.AnonymousIdentity])(nil)
)

// Register declares this instance's definition and assigned region.
//
// Events are the names a browser may send; Internal are the names only a
// declared stream delivers, which the host routes without registering.
func (instance *ClusterHeartbeats[I]) Register() widget.Registration {
	return widget.Registration{
		Name:     ClusterHeartbeatsName,
		Region:   instance.region,
		Events:   []string{ClusterHeartbeatsEventToggleMotion},
		Internal: []string{ClusterHeartbeatsEventSnapshot},
		Streams: []widget.StreamDeclaration{
			{Name: "clusterWatch", Source: "widget.cluster.watch", Delivers: ClusterHeartbeatsEventSnapshot},
		},
		Payloads: []widget.EventPayload{
			{Event: ClusterHeartbeatsEventSnapshot, Fields: []string{
				ClusterHeartbeatsEventSnapshotFieldSequence,
				ClusterHeartbeatsEventSnapshotFieldConnected,
				ClusterHeartbeatsEventSnapshotFieldAuthoritative,
				ClusterHeartbeatsEventSnapshotFieldLeaderKnown,
				ClusterHeartbeatsEventSnapshotFieldHasQuorum,
				ClusterHeartbeatsEventSnapshotFieldTerm,
				ClusterHeartbeatsEventSnapshotFieldVoters,
				ClusterHeartbeatsEventSnapshotFieldAliveVoters,
			}},
		},
	}
}

// Mount opens one session's copy of the widget. It schedules no effect: the
// streams this widget declared are the host's to open, because a widget document
// names no host, no address and no credential.
func (instance *ClusterHeartbeats[I]) Mount(
	ctx context.Context, session live.Session[I],
) (ClusterHeartbeatsState, []live.Effect[I], error) {
	return ClusterHeartbeatsState{}, nil, nil
}

// Reduce is the pure transition from one state to the next. It performs no I/O,
// reads no clock and mutates nothing it was given.
func (instance *ClusterHeartbeats[I]) Reduce(
	state ClusterHeartbeatsState, event live.Event,
) (ClusterHeartbeatsState, []live.Effect[I]) {
	current := state
	switch event.Name {
	case ClusterHeartbeatsEventToggleMotion:
		current.Paused = !current.Paused
	case ClusterHeartbeatsEventSnapshot:
		if raw, present := event.Fields.Lookup(ClusterHeartbeatsEventSnapshotFieldSequence); present {
			current.Sequence = widget.ParseCounter(raw, current.Sequence)
		}
		if raw, present := event.Fields.Lookup(ClusterHeartbeatsEventSnapshotFieldConnected); present {
			current.Connected = widget.ParseFlag(raw, current.Connected)
		}
		if raw, present := event.Fields.Lookup(ClusterHeartbeatsEventSnapshotFieldAuthoritative); present {
			current.Authoritative = widget.ParseFlag(raw, current.Authoritative)
		}
		if raw, present := event.Fields.Lookup(ClusterHeartbeatsEventSnapshotFieldLeaderKnown); present {
			current.LeaderKnown = widget.ParseFlag(raw, current.LeaderKnown)
		}
		if raw, present := event.Fields.Lookup(ClusterHeartbeatsEventSnapshotFieldHasQuorum); present {
			current.HasQuorum = widget.ParseFlag(raw, current.HasQuorum)
		}
		if raw, present := event.Fields.Lookup(ClusterHeartbeatsEventSnapshotFieldTerm); present {
			current.Term = widget.ParseCount(raw, current.Term)
		}
		if raw, present := event.Fields.Lookup(ClusterHeartbeatsEventSnapshotFieldVoters); present {
			current.Voters = widget.ParseCount(raw, current.Voters)
		}
		if raw, present := event.Fields.Lookup(ClusterHeartbeatsEventSnapshotFieldAliveVoters); present {
			current.AliveVoters = widget.ParseCount(raw, current.AliveVoters)
		}
	case live.SlowClientEvent:
		current.Degraded = true
	case live.ClientRecoveredEvent:
		current.Degraded = false
	}
	return current, nil
}

// Render draws the widget's live region. It is a pure function of state:
// equal state renders byte-identical markup.
func (instance *ClusterHeartbeats[I]) Render(state ClusterHeartbeatsState) templ.Component {
	return ClusterHeartbeatsViewAt(state, instance.region)
}

// Dirty reports whether a transition may have changed this widget's markup: the
// state fields its bindings, its predicates and its tick read, and no others.
func (instance *ClusterHeartbeats[I]) Dirty(previous ClusterHeartbeatsState, next ClusterHeartbeatsState) bool {
	return previous.Sequence != next.Sequence ||
		previous.Connected != next.Connected ||
		previous.Authoritative != next.Authoritative ||
		previous.LeaderKnown != next.LeaderKnown ||
		previous.HasQuorum != next.HasQuorum ||
		previous.Paused != next.Paused ||
		previous.Term != next.Term ||
		previous.Voters != next.Voters ||
		previous.AliveVoters != next.AliveVoters ||
		previous.Degraded != next.Degraded
}

// Unmount releases what the session held. This widget holds nothing.
func (instance *ClusterHeartbeats[I]) Unmount(ctx context.Context, session live.Session[I], state ClusterHeartbeatsState) {
}

// Snapshot projects state into ordered name/value pairs, in state-field
// declaration order.
func (instance *ClusterHeartbeats[I]) Snapshot(state ClusterHeartbeatsState) widget.Snapshot {
	return widget.Snapshot{
		Widget: ClusterHeartbeatsName,
		Fields: []widget.SnapshotField{
			{Name: "sequence", Value: strconv.FormatUint(state.Sequence, 10)},
			{Name: "connected", Value: strconv.FormatBool(state.Connected)},
			{Name: "authoritative", Value: strconv.FormatBool(state.Authoritative)},
			{Name: "leaderKnown", Value: strconv.FormatBool(state.LeaderKnown)},
			{Name: "hasQuorum", Value: strconv.FormatBool(state.HasQuorum)},
			{Name: "paused", Value: strconv.FormatBool(state.Paused)},
			{Name: "term", Value: strconv.FormatInt(state.Term, 10)},
			{Name: "voters", Value: strconv.FormatInt(state.Voters, 10)},
			{Name: "aliveVoters", Value: strconv.FormatInt(state.AliveVoters, 10)},
			{Name: "degraded", Value: strconv.FormatBool(state.Degraded)},
		},
	}
}

// Live is the "live" predicate.
func (state ClusterHeartbeatsState) Live() bool {
	return state.Connected && state.Authoritative && state.LeaderKnown && state.HasQuorum && !state.Degraded
}

// QuorumKnown is the "quorumKnown" predicate.
func (state ClusterHeartbeatsState) QuorumKnown() bool {
	return state.Connected && state.Voters >= 1
}

// LeaderTitleText is the "leaderTitleText" binding. The first matching clause wins and the
// otherwise arm is always present, so it is total.
func (state ClusterHeartbeatsState) LeaderTitleText() string {
	if !state.Connected {
		return "leader unavailable"
	}
	if state.LeaderKnown {
		return "elected leader"
	}
	return "leader pending"
}

// LeaderCaptionText is the "leaderCaptionText" binding. The first matching clause wins and the
// otherwise arm is always present, so it is total.
func (state ClusterHeartbeatsState) LeaderCaptionText() string {
	if !state.Connected {
		return "awaiting the fleet view"
	}
	if state.LeaderKnown {
		return "term + fleet view"
	}
	return "election in progress"
}

// SceneDescriptionText is the "sceneDescriptionText" binding. The first matching clause wins and the
// otherwise arm is always present, so it is total.
func (state ClusterHeartbeatsState) SceneDescriptionText() string {
	if !state.Connected {
		return "The stream is unavailable; the stopped diagram shows three generic voters only to illustrate the protocol."
	}
	if state.LeaderKnown {
		return "Privacy-filtered activity showing one elected leader and two generic voting followers; every pulse follows an accepted stream update."
	}
	return "Privacy-filtered view showing three generic voters while a leader election is in progress."
}

// ConnectionStatusText is the "connectionStatusText" binding. The first matching clause wins and the
// otherwise arm is always present, so it is total.
func (state ClusterHeartbeatsState) ConnectionStatusText() string {
	if !state.Connected {
		return "stream reconnecting"
	}
	if state.Degraded {
		return "browser catching up"
	}
	if !state.LeaderKnown {
		return "leader election in progress"
	}
	if !state.Authoritative {
		return "non-authoritative view"
	}
	if !state.HasQuorum {
		return "quorum degraded"
	}
	return "live snapshot"
}

// MotionCaptionText is the "motionCaptionText" binding. The first matching clause wins and the
// otherwise arm is always present, so it is total.
func (state ClusterHeartbeatsState) MotionCaptionText() string {
	if state.Paused {
		return "Resume live pulses"
	}
	return "Pause live pulses"
}

// TermStatText is the "termStatText" binding. The first matching clause wins and the
// otherwise arm is always present, so it is total.
func (state ClusterHeartbeatsState) TermStatText() string {
	if !state.Connected {
		return "term —"
	}
	return "term " + strconv.FormatInt(state.Term, 10)
}

// QuorumStatText is the "quorumStatText" binding. The first matching clause wins and the
// otherwise arm is always present, so it is total.
func (state ClusterHeartbeatsState) QuorumStatText() string {
	if state.QuorumKnown() {
		return strconv.FormatInt(state.AliveVoters, 10) + " / " + strconv.FormatInt(state.Voters, 10) + " voters alive"
	}
	return "quorum —"
}

// TelemetryStatText is the "telemetryStatText" binding. The first matching clause wins and the
// otherwise arm is always present, so it is total.
func (state ClusterHeartbeatsState) TelemetryStatText() string {
	if state.Live() {
		return "Live activity · pulses follow stream updates"
	}
	if state.Degraded {
		return "Browser catching up · live pulses paused"
	}
	if state.Connected {
		return "View degraded · live pulses stopped"
	}
	return "Stream unavailable · live pulses stopped"
}

// WidgetTitleLabel is the "widgetTitleLabel" label.
func (state ClusterHeartbeatsState) WidgetTitleLabel() string {
	return "Raft-style heartbeats"
}

// WidgetSourceLabel is the "widgetSourceLabel" label.
func (state ClusterHeartbeatsState) WidgetSourceLabel() string {
	return "Read-only protobuf stream"
}

// SceneDescriptionLabel is the "sceneDescriptionLabel" label.
func (state ClusterHeartbeatsState) SceneDescriptionLabel() string {
	return state.SceneDescriptionText()
}

// NodeALabel is the "nodeALabel" label.
func (state ClusterHeartbeatsState) NodeALabel() string {
	return "node-a"
}

// NodeACaptionLabel is the "nodeACaptionLabel" label.
func (state ClusterHeartbeatsState) NodeACaptionLabel() string {
	return "follower"
}

// NodeBLabel is the "nodeBLabel" label.
func (state ClusterHeartbeatsState) NodeBLabel() string {
	return state.LeaderTitleText()
}

// NodeBCaptionLabel is the "nodeBCaptionLabel" label.
func (state ClusterHeartbeatsState) NodeBCaptionLabel() string {
	return state.LeaderCaptionText()
}

// NodeCLabel is the "nodeCLabel" label.
func (state ClusterHeartbeatsState) NodeCLabel() string {
	return "node-c"
}

// NodeCCaptionLabel is the "nodeCCaptionLabel" label.
func (state ClusterHeartbeatsState) NodeCCaptionLabel() string {
	return "follower"
}

// HeartbeatLegendLabel is the "heartbeatLegendLabel" label.
func (state ClusterHeartbeatsState) HeartbeatLegendLabel() string {
	return "heartbeat"
}

// AckLegendLabel is the "ackLegendLabel" label.
func (state ClusterHeartbeatsState) AckLegendLabel() string {
	return "ack"
}

// ConnectionStatusLabel is the "connectionStatusLabel" label.
func (state ClusterHeartbeatsState) ConnectionStatusLabel() string {
	return state.ConnectionStatusText()
}

// MotionCaptionLabel is the "motionCaptionLabel" label.
func (state ClusterHeartbeatsState) MotionCaptionLabel() string {
	return state.MotionCaptionText()
}

// TermStatLabel is the "termStatLabel" label.
func (state ClusterHeartbeatsState) TermStatLabel() string {
	return state.TermStatText()
}

// QuorumStatLabel is the "quorumStatLabel" label.
func (state ClusterHeartbeatsState) QuorumStatLabel() string {
	return state.QuorumStatText()
}

// TelemetryStatLabel is the "telemetryStatLabel" label.
func (state ClusterHeartbeatsState) TelemetryStatLabel() string {
	return state.TelemetryStatText()
}

// MotionActive is this widget's compiled motion gate: everything its motion
// block requires, and none of what it forbids. The host's own connection status
// is the gate's other half and is applied by the stylesheet, so an animation
// cannot run against a dead connection whatever this returns.
func (state ClusterHeartbeatsState) MotionActive() bool {
	return state.Live() && !state.Paused
}

// MotionActiveText renders the gate into the attribute the stylesheet selects on.
func (state ClusterHeartbeatsState) MotionActiveText() string {
	return strconv.FormatBool(state.MotionActive())
}

// MotionTickID is the scene's identity for one tick.
//
// Every animation is finite and none repeats, so what starts one again is a new
// element: the scene carries this as its id, the tick advances, and the picture
// moves exactly as often as the data does.
func (state ClusterHeartbeatsState) MotionTickID() string {
	return state.MotionTickIDAt(ClusterHeartbeatsRegion)
}

// MotionTickIDAt namespaces the scene's tick identity under its live instance.
func (state ClusterHeartbeatsState) MotionTickIDAt(region string) string {
	return region + "-tick-" + strconv.FormatUint(state.Sequence, 10)
}

// ConnectionTone is the "connection" indicator's tone: positive while its predicate holds, warning otherwise.
func (state ClusterHeartbeatsState) ConnectionTone() string {
	if state.Live() {
		return "positive"
	}
	return "warning"
}

// MotionTogglePressedText renders the "motionToggle" control's pressed state.
func (state ClusterHeartbeatsState) MotionTogglePressedText() string {
	return strconv.FormatBool(state.Paused)
}
