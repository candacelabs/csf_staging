// Package candacenet.warden.v1 is the Protocol-Buffers source of truth for the
// candacenet warden node-to-node wire contracts: the cluster RPCs
// (vote/heartbeat/identify), the server-streaming cluster watch, and the
// durable persistent-state record.
//
// -----------------------------------------------------------------------------
// Field-number policy (this file is the authority; numbers are the only literals
// permitted in this schema, by mandate)
// -----------------------------------------------------------------------------
//   * Numbers 1-15 encode as a single tag byte on the binary wire and are
//     reserved for the current, frequently-set ("hot") fields of every message.
//     Every message defined here fits entirely within the 1-15 band today.
//   * Field numbers are assigned in declaration order starting at 1. Declaration
//     order is also protojson's field-output order, so keeping declaration order
//     aligned with the historical Go struct order keeps the JSON rendering
//     familiar.
//   * A shipped field number is permanent: never renumber it and never reuse it
//     for a different field. Removing a field REQUIRES a `reserved <number>;` and
//     `reserved "<name>";` declaration in the same message so no later field can
//     silently reoccupy the slot or name.
//   * Growth is additive-only within a version: new fields take the next free
//     number (spilling past 15 only once the hot band is full). Any breaking
//     change (renumber, retype, delete-without-reserve, semantic reinterpretation)
//     requires a new package version directory (candacenet.warden.v2).
//   * `buf breaking` enforces this mechanically against the last committed schema.
//
// -----------------------------------------------------------------------------
// Relationship to the current JSON wire (services/warden: wire.go, types.go)
// -----------------------------------------------------------------------------
// This schema mirrors the frozen Go wire types 1:1 in field names and meaning.
// It is the source of truth for the forthcoming gRPC transport; the existing
// HTTP/JSON transport and its contract goldens are intentionally left in place
// and unchanged. Where a follow-up worker renders these messages with protojson,
// canonical protobuf-JSON differs from today's encoding/json output in ways that
// are inherent to the proto3 JSON mapping and are accepted, documented deltas:
//   * 64-bit integers (uint64 term/version/…) render as JSON *strings*
//     ("7"), not bare numbers (7). This is mandatory proto3 JSON and unavoidable.
//   * Zero-valued scalar fields are omitted by default (no EmitUnpopulated); the
//     historical encoding/json output emits them (e.g. "term":0). A DiscardUnknown
//     reader reconstructs the zero, so the semantics are identical.
//   * snake_case names are preserved ONLY when the renderer sets
//     protojson.MarshalOptions{UseProtoNames: true}; the default emits lowerCamel
//     (candidateId). No json_name overrides are used because UseProtoNames covers
//     the whole schema uniformly.
//   * timestamps render per google.protobuf.Timestamp (RFC 3339, 0/3/6/9
//     fractional digits) and are omitted entirely when unset, rather than
//     appearing as the Go zero instant "0001-01-01T00:00:00Z".

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: warden/v1/warden.proto

// buf:lint:ignore PACKAGE_DIRECTORY_MATCH
// The brief fixes both the package (candacenet.warden.v1) and the on-disk path
// (services/warden/proto/warden/v1/warden.proto). The path omits the org
// "candacenet" segment that PACKAGE_DIRECTORY_MATCH would require, so the rule is
// intentionally waived for this package. The version-suffixed leaf (warden/v1)
// still guarantees per-version isolation.

package wardenv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	WardenService_Vote_FullMethodName         = "/candacenet.warden.v1.WardenService/Vote"
	WardenService_Heartbeat_FullMethodName    = "/candacenet.warden.v1.WardenService/Heartbeat"
	WardenService_Identify_FullMethodName     = "/candacenet.warden.v1.WardenService/Identify"
	WardenService_WatchCluster_FullMethodName = "/candacenet.warden.v1.WardenService/WatchCluster"
)

// WardenServiceClient is the client API for WardenService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// WardenService is the node-to-node RPC surface. The three unary RPCs mirror the
// existing HTTP endpoints; WatchCluster adds a push-based cluster-view stream
// that a follow-up worker will implement over gRPC. This schema defines the
// contract only — no server or client is generated here beyond the stubs.
type WardenServiceClient interface {
	// Vote requests a peer's vote in an election (mirrors POST /warden/v1/vote).
	Vote(ctx context.Context, in *VoteRequest, opts ...grpc.CallOption) (*VoteResponse, error)
	// Heartbeat asserts leadership and conveys the authoritative view/membership
	// (mirrors POST /warden/v1/heartbeat).
	Heartbeat(ctx context.Context, in *HeartbeatRequest, opts ...grpc.CallOption) (*HeartbeatResponse, error)
	// Identify answers the cluster-identity handshake (mirrors GET
	// /warden/v1/identify).
	Identify(ctx context.Context, in *IdentifyRequest, opts ...grpc.CallOption) (*IdentifyResponse, error)
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	// WatchCluster streams full ClusterView snapshots on every cluster-state
	// change. The response is named for the domain event it carries
	// (ClusterViewUpdate), not the RPC, because it is a recurring push element
	// rather than a one-shot WatchClusterResponse envelope — hence the scoped
	// waiver of the response-naming rule.
	WatchCluster(ctx context.Context, in *WatchClusterRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ClusterViewUpdate], error)
}

type wardenServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewWardenServiceClient(cc grpc.ClientConnInterface) WardenServiceClient {
	return &wardenServiceClient{cc}
}

func (c *wardenServiceClient) Vote(ctx context.Context, in *VoteRequest, opts ...grpc.CallOption) (*VoteResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(VoteResponse)
	err := c.cc.Invoke(ctx, WardenService_Vote_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *wardenServiceClient) Heartbeat(ctx context.Context, in *HeartbeatRequest, opts ...grpc.CallOption) (*HeartbeatResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(HeartbeatResponse)
	err := c.cc.Invoke(ctx, WardenService_Heartbeat_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *wardenServiceClient) Identify(ctx context.Context, in *IdentifyRequest, opts ...grpc.CallOption) (*IdentifyResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(IdentifyResponse)
	err := c.cc.Invoke(ctx, WardenService_Identify_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *wardenServiceClient) WatchCluster(ctx context.Context, in *WatchClusterRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ClusterViewUpdate], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &WardenService_ServiceDesc.Streams[0], WardenService_WatchCluster_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[WatchClusterRequest, ClusterViewUpdate]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type WardenService_WatchClusterClient = grpc.ServerStreamingClient[ClusterViewUpdate]

// WardenServiceServer is the server API for WardenService service.
// All implementations must embed UnimplementedWardenServiceServer
// for forward compatibility.
//
// WardenService is the node-to-node RPC surface. The three unary RPCs mirror the
// existing HTTP endpoints; WatchCluster adds a push-based cluster-view stream
// that a follow-up worker will implement over gRPC. This schema defines the
// contract only — no server or client is generated here beyond the stubs.
type WardenServiceServer interface {
	// Vote requests a peer's vote in an election (mirrors POST /warden/v1/vote).
	Vote(context.Context, *VoteRequest) (*VoteResponse, error)
	// Heartbeat asserts leadership and conveys the authoritative view/membership
	// (mirrors POST /warden/v1/heartbeat).
	Heartbeat(context.Context, *HeartbeatRequest) (*HeartbeatResponse, error)
	// Identify answers the cluster-identity handshake (mirrors GET
	// /warden/v1/identify).
	Identify(context.Context, *IdentifyRequest) (*IdentifyResponse, error)
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	// WatchCluster streams full ClusterView snapshots on every cluster-state
	// change. The response is named for the domain event it carries
	// (ClusterViewUpdate), not the RPC, because it is a recurring push element
	// rather than a one-shot WatchClusterResponse envelope — hence the scoped
	// waiver of the response-naming rule.
	WatchCluster(*WatchClusterRequest, grpc.ServerStreamingServer[ClusterViewUpdate]) error
	mustEmbedUnimplementedWardenServiceServer()
}

// UnimplementedWardenServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedWardenServiceServer struct{}

func (UnimplementedWardenServiceServer) Vote(context.Context, *VoteRequest) (*VoteResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Vote not implemented")
}
func (UnimplementedWardenServiceServer) Heartbeat(context.Context, *HeartbeatRequest) (*HeartbeatResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Heartbeat not implemented")
}
func (UnimplementedWardenServiceServer) Identify(context.Context, *IdentifyRequest) (*IdentifyResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Identify not implemented")
}
func (UnimplementedWardenServiceServer) WatchCluster(*WatchClusterRequest, grpc.ServerStreamingServer[ClusterViewUpdate]) error {
	return status.Error(codes.Unimplemented, "method WatchCluster not implemented")
}
func (UnimplementedWardenServiceServer) mustEmbedUnimplementedWardenServiceServer() {}
func (UnimplementedWardenServiceServer) testEmbeddedByValue()                       {}

// UnsafeWardenServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to WardenServiceServer will
// result in compilation errors.
type UnsafeWardenServiceServer interface {
	mustEmbedUnimplementedWardenServiceServer()
}

func RegisterWardenServiceServer(s grpc.ServiceRegistrar, srv WardenServiceServer) {
	// If the following call panics, it indicates UnimplementedWardenServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&WardenService_ServiceDesc, srv)
}

func _WardenService_Vote_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(VoteRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(WardenServiceServer).Vote(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: WardenService_Vote_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(WardenServiceServer).Vote(ctx, req.(*VoteRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _WardenService_Heartbeat_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HeartbeatRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(WardenServiceServer).Heartbeat(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: WardenService_Heartbeat_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(WardenServiceServer).Heartbeat(ctx, req.(*HeartbeatRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _WardenService_Identify_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(IdentifyRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(WardenServiceServer).Identify(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: WardenService_Identify_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(WardenServiceServer).Identify(ctx, req.(*IdentifyRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _WardenService_WatchCluster_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(WatchClusterRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(WardenServiceServer).WatchCluster(m, &grpc.GenericServerStream[WatchClusterRequest, ClusterViewUpdate]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type WardenService_WatchClusterServer = grpc.ServerStreamingServer[ClusterViewUpdate]

// WardenService_ServiceDesc is the grpc.ServiceDesc for WardenService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var WardenService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "candacenet.warden.v1.WardenService",
	HandlerType: (*WardenServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Vote",
			Handler:    _WardenService_Vote_Handler,
		},
		{
			MethodName: "Heartbeat",
			Handler:    _WardenService_Heartbeat_Handler,
		},
		{
			MethodName: "Identify",
			Handler:    _WardenService_Identify_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "WatchCluster",
			Handler:       _WardenService_WatchCluster_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "warden/v1/warden.proto",
}
