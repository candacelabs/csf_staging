import { createTheme } from "@mantine/core";

// Keep the shell on Mantine's built-in palette. This one token gives primary
// actions and responsive disclosure a clear, consistent accent.
export const workbenchTheme = createTheme({ primaryColor: "orange" });
