// Code generated by pkg/widget/internal/uigen from the KanbanCard widget document. DO NOT EDIT.

package card

import "github.com/candacelabs/csf/pkg/gotth/live"

// KanbanCardView draws the KanbanCard widget's default live region.
templ KanbanCardView(state KanbanCardState) {
	@KanbanCardViewAt(state, KanbanCardRegion)
}

// KanbanCardViewAt draws one instance in its host-assigned live region.
//
// Every value in it is a pure read of state and region: equal inputs render
// byte-identical markup: that comparison is what suppresses a patch nobody needs.
templ KanbanCardViewAt(state KanbanCardState, region string) {
	{{ titleID := region + "-title" }}
	<aside { live.Region(region)... } class="widget" aria-labelledby={ titleID } data-widget="KanbanCard" data-palette="fieldStation">
		<header class="widget-chrome">
			<p class="widget-source widget-token-muted">{ state.DetailLabel() }</p>
			<h2 class="widget-title widget-token-ink" id={ titleID }>{ state.TitleLabel() }</h2>
		</header>
		<svg class="widget-scene" viewBox="0 0 100 100" preserveAspectRatio="xMidYMid meet" role="img" aria-label={ state.DescriptionLabel() }>
			<g class="widget-node widget-role-card" transform="translate(50,50)">
				<circle class="widget-marker widget-marker-large widget-token-accent" r="9"></circle>
				<text class="widget-node-title widget-token-ink" y="-14" text-anchor="middle">{ state.TitleLabel() }</text>
				<text class="widget-node-caption widget-token-muted" y="20" text-anchor="middle">{ state.DetailLabel() }</text>
			</g>
		</svg>
	</aside>
}
