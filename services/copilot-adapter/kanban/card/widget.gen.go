// Code generated by pkg/widget/internal/uigen from the KanbanCard widget document. DO NOT EDIT.

// Package card is the generated KanbanCard widget.
//
// Edit the widget document and regenerate; edits here are overwritten.
package card

import (
	"context"
	"strconv"

	"github.com/a-h/templ"

	"github.com/candacelabs/csf/pkg/gotth/live"
	"github.com/candacelabs/csf/pkg/widget"
)

// This widget's identity. The region is the only one that crosses the wire: a
// patch names it, so changing it is a client-visible change. The palette is a
// name a host resolves to values; this widget carries no colour of its own.
// The title id is what the widget's landmark is labelled by.
const (
	KanbanCardName    = "KanbanCard"
	KanbanCardRegion  = "kanban.card"
	KanbanCardPalette = "fieldStation"
	KanbanCardTitleID = "kanban.card-title"
)

// The wire names this widget accepts. The set is exhaustive and default-deny:
// a name absent from the registration is refused before any reducer runs.
const (
	KanbanCardEventSnapshot = "kanban.card.snapshot"
)

// The wire field names each event carries. They are the other half of an
// event's contract: the widget reads these out of a delivery, and whatever
// produced the delivery wrote them in. Registration.Payloads carries the same
// names as data, for a host that fills an event it cannot name at compile time.
const (
	KanbanCardEventSnapshotFieldName           = "name"
	KanbanCardEventSnapshotFieldSessionId      = "session_id"
	KanbanCardEventSnapshotFieldStatus         = "status"
	KanbanCardEventSnapshotFieldModel          = "model"
	KanbanCardEventSnapshotFieldWorktree       = "worktree"
	KanbanCardEventSnapshotFieldTaskUrl        = "task_url"
	KanbanCardEventSnapshotFieldCheckpointId   = "checkpoint_id"
	KanbanCardEventSnapshotFieldCheckpointUrl  = "checkpoint_url"
	KanbanCardEventSnapshotFieldTaskStatus     = "task_status"
	KanbanCardEventSnapshotFieldOwner          = "owner"
	KanbanCardEventSnapshotFieldNextAction     = "next_action"
	KanbanCardEventSnapshotFieldCondition      = "condition"
	KanbanCardEventSnapshotFieldEvidenceUrl    = "evidence_url"
	KanbanCardEventSnapshotFieldLinkGeneration = "link_generation"
)

// KanbanCardState is this widget's own state, and the type argument every
// method below is written in. The host holds it without knowing it; nothing
// in this file asserts it back, because nothing in this file ever loses it.
type KanbanCardState struct {
	// Name is the "name" state field, declared type text.
	Name string

	// SessionID is the "sessionID" state field, declared type text.
	SessionID string

	// Status is the "status" state field, declared type text.
	Status string

	// Model is the "model" state field, declared type text.
	Model string

	// Worktree is the "worktree" state field, declared type text.
	Worktree string

	// TaskURL is the "taskURL" state field, declared type text.
	TaskURL string

	// CheckpointID is the "checkpointID" state field, declared type text.
	CheckpointID string

	// CheckpointURL is the "checkpointURL" state field, declared type text.
	CheckpointURL string

	// TaskStatus is the "taskStatus" state field, declared type text.
	TaskStatus string

	// Owner is the "owner" state field, declared type text.
	Owner string

	// NextAction is the "nextAction" state field, declared type text.
	NextAction string

	// Condition is the "condition" state field, declared type text.
	Condition string

	// EvidenceURL is the "evidenceURL" state field, declared type text.
	EvidenceURL string

	// LinkGeneration is the "linkGeneration" state field, declared type counter.
	LinkGeneration uint64
}

// KanbanCard is the generated KanbanCard widget.
//
// I is the HOST's identity type, threaded through and never read: a widget
// document names no host, no address and no credential, so nothing here can
// look at an identity. It is a type parameter because the SDK's contract
// carries one — live.Session stopped erasing the application's identity type
// on 2026-09-03 — and a generated widget must fit whatever host registers it.
type KanbanCard[I live.IIdentity] struct {
	region string
}

// NewKanbanCard returns the widget a host registers, instantiated on that
// host's own identity type.
func NewKanbanCard[I live.IIdentity]() *KanbanCard[I] { return NewKanbanCardAt[I](KanbanCardRegion) }

// NewKanbanCardAt gives one instance its host-assigned region. The host validates
// region syntax and uniqueness when assembling its live configuration.
func NewKanbanCardAt[I live.IIdentity](region string) *KanbanCard[I] {
	return &KanbanCard[I]{region: region}
}

// The contract, asserted at one instantiation. Anonymous is the identity a
// host with no accounts uses, and any other I satisfies the same interfaces:
// nothing below branches on it.
var (
	_ widget.IWidget[KanbanCardState, live.AnonymousIdentity] = (*KanbanCard[live.AnonymousIdentity])(nil)
	_ widget.IDirtyDeclarer[KanbanCardState]                  = (*KanbanCard[live.AnonymousIdentity])(nil)
)

// Register declares this instance's definition and assigned region.
//
// Events are the names a browser may send; Internal are the names only a
// declared stream delivers, which the host routes without registering.
func (instance *KanbanCard[I]) Register() widget.Registration {
	return widget.Registration{
		Name:     KanbanCardName,
		Region:   instance.region,
		Internal: []string{KanbanCardEventSnapshot},
		Streams: []widget.StreamDeclaration{
			{Name: "changes", Source: "workspace.committed", Delivers: KanbanCardEventSnapshot},
		},
		Payloads: []widget.EventPayload{
			{Event: KanbanCardEventSnapshot, Fields: []string{
				KanbanCardEventSnapshotFieldName,
				KanbanCardEventSnapshotFieldSessionId,
				KanbanCardEventSnapshotFieldStatus,
				KanbanCardEventSnapshotFieldModel,
				KanbanCardEventSnapshotFieldWorktree,
				KanbanCardEventSnapshotFieldTaskUrl,
				KanbanCardEventSnapshotFieldCheckpointId,
				KanbanCardEventSnapshotFieldCheckpointUrl,
				KanbanCardEventSnapshotFieldTaskStatus,
				KanbanCardEventSnapshotFieldOwner,
				KanbanCardEventSnapshotFieldNextAction,
				KanbanCardEventSnapshotFieldCondition,
				KanbanCardEventSnapshotFieldEvidenceUrl,
				KanbanCardEventSnapshotFieldLinkGeneration,
			}},
		},
	}
}

// Mount opens one session's copy of the widget. It schedules no effect: the
// streams this widget declared are the host's to open, because a widget document
// names no host, no address and no credential.
func (instance *KanbanCard[I]) Mount(
	ctx context.Context, session live.Session[I],
) (KanbanCardState, []live.Effect[I], error) {
	return KanbanCardState{}, nil, nil
}

// Reduce is the pure transition from one state to the next. It performs no I/O,
// reads no clock and mutates nothing it was given.
func (instance *KanbanCard[I]) Reduce(
	state KanbanCardState, event live.Event,
) (KanbanCardState, []live.Effect[I]) {
	current := state
	switch event.Name {
	case KanbanCardEventSnapshot:
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldName); present {
			current.Name = raw
		}
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldSessionId); present {
			current.SessionID = raw
		}
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldStatus); present {
			current.Status = raw
		}
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldModel); present {
			current.Model = raw
		}
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldWorktree); present {
			current.Worktree = raw
		}
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldTaskUrl); present {
			current.TaskURL = raw
		}
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldCheckpointId); present {
			current.CheckpointID = raw
		}
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldCheckpointUrl); present {
			current.CheckpointURL = raw
		}
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldTaskStatus); present {
			current.TaskStatus = raw
		}
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldOwner); present {
			current.Owner = raw
		}
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldNextAction); present {
			current.NextAction = raw
		}
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldCondition); present {
			current.Condition = raw
		}
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldEvidenceUrl); present {
			current.EvidenceURL = raw
		}
		if raw, present := event.Fields.Lookup(KanbanCardEventSnapshotFieldLinkGeneration); present {
			current.LinkGeneration = widget.ParseCounter(raw, current.LinkGeneration)
		}
	}
	return current, nil
}

// Render draws the widget's live region. It is a pure function of state:
// equal state renders byte-identical markup.
func (instance *KanbanCard[I]) Render(state KanbanCardState) templ.Component {
	return KanbanCardViewAt(state, instance.region)
}

// Dirty reports whether a transition may have changed this widget's markup: the
// state fields its bindings, its predicates and its tick read, and no others.
func (instance *KanbanCard[I]) Dirty(previous KanbanCardState, next KanbanCardState) bool {
	return previous.Name != next.Name ||
		previous.SessionID != next.SessionID ||
		previous.Status != next.Status ||
		previous.Model != next.Model ||
		previous.Worktree != next.Worktree ||
		previous.TaskURL != next.TaskURL ||
		previous.CheckpointID != next.CheckpointID ||
		previous.CheckpointURL != next.CheckpointURL ||
		previous.TaskStatus != next.TaskStatus ||
		previous.Owner != next.Owner ||
		previous.NextAction != next.NextAction ||
		previous.Condition != next.Condition ||
		previous.EvidenceURL != next.EvidenceURL ||
		previous.LinkGeneration != next.LinkGeneration
}

// Unmount releases what the session held. This widget holds nothing.
func (instance *KanbanCard[I]) Unmount(ctx context.Context, session live.Session[I], state KanbanCardState) {
}

// Snapshot projects state into ordered name/value pairs, in state-field
// declaration order.
func (instance *KanbanCard[I]) Snapshot(state KanbanCardState) widget.Snapshot {
	return widget.Snapshot{
		Widget: KanbanCardName,
		Fields: []widget.SnapshotField{
			{Name: "name", Value: state.Name},
			{Name: "sessionID", Value: state.SessionID},
			{Name: "status", Value: state.Status},
			{Name: "model", Value: state.Model},
			{Name: "worktree", Value: state.Worktree},
			{Name: "taskURL", Value: state.TaskURL},
			{Name: "checkpointID", Value: state.CheckpointID},
			{Name: "checkpointURL", Value: state.CheckpointURL},
			{Name: "taskStatus", Value: state.TaskStatus},
			{Name: "owner", Value: state.Owner},
			{Name: "nextAction", Value: state.NextAction},
			{Name: "condition", Value: state.Condition},
			{Name: "evidenceURL", Value: state.EvidenceURL},
			{Name: "linkGeneration", Value: strconv.FormatUint(state.LinkGeneration, 10)},
		},
	}
}

// TitleLabel is the "titleLabel" label.
func (state KanbanCardState) TitleLabel() string {
	return state.Name
}

// DetailLabel is the "detailLabel" label.
func (state KanbanCardState) DetailLabel() string {
	return state.SessionID + " · " + state.Status + " · " + state.Model + " · " + state.Worktree + " · " + state.TaskURL + " · " + state.CheckpointID + " · " + state.CheckpointURL + " · " + state.TaskStatus + " · " + state.Owner + " · " + state.NextAction + " · " + state.Condition + " · " + state.EvidenceURL + " · " + strconv.FormatUint(state.LinkGeneration, 10)
}

// DescriptionLabel is the "descriptionLabel" label.
func (state KanbanCardState) DescriptionLabel() string {
	return "Session and authoritative task checkpoint"
}
